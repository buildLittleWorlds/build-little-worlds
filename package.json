{
  "name": "build-little-worlds",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "worker:dev": "wrangler dev worker/src/index.js",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
